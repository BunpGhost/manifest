import { type Component } from 'solid-js';

// Empty component — routing deprecation notice is disabled in this fork.
const RoutingDeprecationNotice: Component = () => null;

export default RoutingDeprecationNotice;
